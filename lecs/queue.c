#include <stdio.h>
#include <stdlib.h>

//circular array implementation - size of array is number of elements then use 
//index = (front_index + curr_num_items)%max_size
//dequeue and unqueue are both O(1)

//linked list implementation both O(1) 

int main(){
    
    return 0;
}